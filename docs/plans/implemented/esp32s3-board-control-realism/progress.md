# ESP32-S3 Board Control Realism Progress

- 2026-04-19: Promoted plan `#1` from `docs/plans/new/` to `docs/plans/in-progress/` on `codex/esp32s3-board-control-realism`.
- 2026-04-19: Set the active evidence targets for this phase: local sleep reference, local TRM corpus, ESP-IDF clock/PMU/low-power sources, and the existing RTC/system/TIMG/sleep regression set.
- 2026-04-19: Reconstructed the first bounded board-control slice from the local sleep reference: save CPU/system clock state, switch `SYSTEM_SYSCLK_CONF.SOC_CLK_SEL` from PLL to XTAL for RTC light sleep, then refine wake to the adjacent firmware-visible clock contract instead of blindly restoring the runtime tier.
- 2026-04-19: Landed the light-sleep system-clock handoff in `hw/xtensa/esp32s3_clk.c` and the SoC light-sleep hook in `hw/xtensa/esp32s3.c`, keeping clock-consumer fanout on the existing `clock-update` path.
- 2026-04-19: Tightened the wake-side clock ownership boundary in `hw/xtensa/esp32s3_clk.c`: light sleep now resumes with PLL selected in `SYSTEM_SYSCLK_CONF` and the wake-stub boot `SYSTEM_CPU_PER_CONF` contract (160 MHz PLL) so firmware `restore_after_wake()` owns runtime tier reapplication.
- 2026-04-19: Replaced the first RTC clock handoff assertion with direct qtest coverage for the firmware-visible wake contract at `/xtensa/esp32s3/rtc/light-sleep-restores-wake-clock-contract`, keeping the existing RTC light-sleep SYSTIMER-stop coverage alongside it.
- 2026-04-19: Verified the refined wake clock contract with `git diff --check`, `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`, the `/xtensa/esp32s3/rtc`, `/xtensa/esp32s3/system`, and `/xtensa/esp32s3/timg` qtest groups, the full `esp32s3-test` qtest binary, and the functional `sleep_wake` and `cache_reject` regressions.
- 2026-04-19: Promoted `RTC_CNTL_EXT_WAKEUP1_STATUS` out of the gap doc by latching the EXT1 trigger bitmap on immediate and in-sleep EXT1 wake paths and honoring the write-only `EXT_WAKEUP1_STATUS_CLR` bit in `RTC_CNTL_EXT_WAKEUP1`.
- 2026-04-19: Added direct qtest coverage for the new RTC latch contract at `/xtensa/esp32s3/rtc/ext1-status-latch` and re-verified the full `/xtensa/esp32s3/rtc` subgroup after widening the RTC register surface.
- 2026-04-19: Re-verified the full board-control floor after the EXT1 status slice with `git diff --check`, `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`, the `/xtensa/esp32s3/rtc`, `/xtensa/esp32s3/system`, and `/xtensa/esp32s3/timg` qtest groups, the full `esp32s3-test` qtest binary, and the functional `sleep_wake` and `cache_reject` regressions.
- 2026-04-19: Promoted `RTC_CNTL_TIMER1` out of the unsupported RTC bucket as an explicit source-backed register surface with `PLL_BUF_WAIT`, `XTL_BUF_WAIT`, and `CK8M_WAIT` masks and reset defaults. This is a register-contract closure only; wake settling latency remains deliberately unmodeled.
- 2026-04-19: Extended the RTC explicit-surface and reset-default qtests to include `RTC_CNTL_TIMER1`, then re-verified the `/xtensa/esp32s3/rtc` subgroup after widening the light-sleep config surface again.
- 2026-04-19: Re-verified the full board-control floor after the `RTC_CNTL_TIMER1` slice with `git diff --check`, `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`, the `/xtensa/esp32s3/rtc`, `/xtensa/esp32s3/system`, and `/xtensa/esp32s3/timg` qtest groups, the full `esp32s3-test` qtest binary, and the functional `sleep_wake` and `cache_reject` regressions.
- 2026-04-19: Promoted `RTC_CNTL_SLP_REJECT_CAUSE` out of the unsupported RTC bucket with a deliberately narrow contract: the current GPIO low-level reject path now latches `RTC_GPIO_TRIG_EN`, and `SLP_REJECT_CAUSE_CLR` clears that latched cause when the reject state is released.
- 2026-04-19: Extended the RTC explicit-surface and GPIO reject qtests to cover the new reject-cause register contract, then re-verified the `/xtensa/esp32s3/rtc` subgroup after widening the RTC sleep-status surface again.
- 2026-04-19: Re-verified the full board-control floor after the reject-cause slice with `git diff --check`, `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`, the `/xtensa/esp32s3/rtc`, `/xtensa/esp32s3/system`, and `/xtensa/esp32s3/timg` qtest groups, the full `esp32s3-test` qtest binary, and the functional `sleep_wake` and `cache_reject` regressions.
- 2026-04-19: Promoted `RTC_CNTL_RETENTION_CTRL` out of the unsupported RTC bucket as an explicit source-backed register surface with TRM reset defaults and write masks for `RETENTION_WAIT`, `RETENTION_EN`, `RETENTION_CLKOFF_WAIT`, `RETENTION_DONE_WAIT`, `RETENTION_CLK_SEL`, `RETENTION_TARGET`, and `RETENTION_TAG_MODE`.
- 2026-04-19: Kept the retention closure narrow: this slice only models the documented register contract and reset defaults, not CPU-retention DMA descriptors, retention-memory save/restore, or retention timing side effects.
- 2026-04-19: Replaced the old unsupported `RETENTION_CTRL` qtest sentinel with explicit retention-default and write-mask coverage, moved the unsupported sentinel forward to `RTC_CNTL_PG_CTRL`, and re-verified the `/xtensa/esp32s3/rtc` subgroup after widening the RTC low-power control surface again.
- 2026-04-19: Re-verified the full board-control floor after the `RTC_CNTL_RETENTION_CTRL` slice with `git diff --check`, `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`, the `/xtensa/esp32s3/rtc`, `/xtensa/esp32s3/system`, and `/xtensa/esp32s3/timg` qtest groups, the full `esp32s3-test` qtest binary, and the functional `sleep_wake` and `cache_reject` regressions.
- 2026-04-19: Promoted `RTC_CNTL_PG_CTRL` out of the unsupported RTC bucket as an explicit source-backed register surface with write masks for `POWER_GLITCH_EN`, `POWER_GLITCH_EFUSE_SEL`, `POWER_GLITCH_FORCE_PU`, `POWER_GLITCH_FORCE_PD`, and `POWER_GLITCH_DSENSE`.
- 2026-04-19: Kept the power-glitch closure narrow: this slice models only the documented register contract and reset default, not analog FIB/power-glitch behavior, brownout side effects, or power-domain sequencing.
- 2026-04-19: Replaced the old unsupported `PG_CTRL` qtest sentinel with explicit write-mask coverage, moved the unsupported sentinel forward to `RTC_CNTL_FIB_SEL`, and re-verified the `/xtensa/esp32s3/rtc` subgroup after widening the RTC low-power control surface again.
- 2026-04-19: Re-verified the full board-control floor after the `RTC_CNTL_PG_CTRL` slice with `git diff --check`, `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`, the `/xtensa/esp32s3/rtc`, `/xtensa/esp32s3/system`, and `/xtensa/esp32s3/timg` qtest groups, the full `esp32s3-test` qtest binary, and the functional `sleep_wake` and `cache_reject` regressions.
- 2026-04-19: Promoted the remaining source-backed RTC tail at `0x148`-`0x154`: `RTC_CNTL_FIB_SEL`, `RTC_CNTL_TOUCH_DAC`, `RTC_CNTL_TOUCH_DAC1`, and `RTC_CNTL_COCPU_DISABLE` now expose their documented reset defaults and write masks instead of falling through the unsupported RTC bucket.
- 2026-04-19: Kept the tail-surface closure narrow: `FIB_SEL` is register storage only and does not manufacture brownout/glitch resets, the touch DAC words do not create touch-sensor analog behavior, and `COCPU_DISABLE` does not instantiate or schedule an RTC coprocessor.
- 2026-04-19: Replaced the old unsupported `FIB_SEL` qtest sentinel with explicit tail-surface coverage, moved the unsupported sentinel forward to `RTC_CNTL_INT_ENA_W1TC`, and re-verified the `/xtensa/esp32s3/rtc` subgroup after closing the last adjacent RTC tail controls.
- 2026-04-19: Re-verified the full board-control floor after the RTC tail-surface closure with `git diff --check`, `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`, the `/xtensa/esp32s3/rtc`, `/xtensa/esp32s3/system`, and `/xtensa/esp32s3/timg` qtest groups, the full `esp32s3-test` qtest binary, and the functional `sleep_wake` and `cache_reject` regressions.
- 2026-04-19: Closed plan `#1` as implemented. The remaining board-control items are now explicitly documented as source-gated behavior work or future-plan scope rather than active unfinished slices in this plan.
