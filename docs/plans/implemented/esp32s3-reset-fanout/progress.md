# ESP32-S3 Reset Fanout Progress

- 2026-04-18: Started the follow-on reset-fanout phase in `/Users/skooch/projects/tdeck-pro-rust/worktrees/esp32s3-reset-fanout-phase3` from `skooch/tdeck-peripherals`.
- 2026-04-18: Confirmed the current SoC helper still cold-resets only the interrupt matrix, UARTs, and I2C controllers on digital reset, matching the repo-root documented gap.
- 2026-04-18: Selected the next slice around the already owned digital devices with direct qtest-observable contracts instead of trigger-gated cache/Xtensa backlog or source-blocked EMAC/RMT work.
- 2026-04-18: Replaced the narrow peripheral-reset helper with an owned-digital reset helper that cold-resets the interrupt matrix, UARTs, GPIO, RNG, I2C, SPI0/SPI1, EXTMEM cache/MMU, SYSTEM clock block, GDMA, SHA/AES/RSA/HMAC/DS/PMS/XTS_AES, TIMG0/TIMG1, SYSTIMER, USB Serial/JTAG, IO_MUX, GPSPI, and the existing analog register shims.
- 2026-04-18: Kept CPU reset ownership unchanged. Software digital reset now uses the widened owned-digital helper and then reapplies the retained RTC clock selection into the SYSTEM clock model. Host/QMP full-chip reset still cold-resets RTC_CNTL first, then runs the same owned-digital helper so the existing host-reset clock defaults remain intact.
- 2026-04-18: Extended `test_rtc_reset_transitions()` to dirty and verify representative resettable state across the SYSTEM clock block, EXTMEM cache/MMU, GPIO/IO_MUX, GPSPI, SHA, PMS, and TIMG. The qtest now proves CPU-only resets preserve dirty digital state while software digital reset and host full-chip reset clear the widened owned surface to their expected defaults.
- 2026-04-18: Verification passed: `git diff --check`; `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`; `QTEST_QEMU_BINARY=./build/qemu-system-xtensa ./build/tests/qtest/esp32s3-test -p /xtensa/esp32s3/rtc/reset-transitions`; `QTEST_QEMU_BINARY=./build/qemu-system-xtensa ./build/tests/qtest/esp32s3-test -p /xtensa/esp32s3/rtc`; `QTEST_QEMU_BINARY=./build/qemu-system-xtensa ./build/tests/qtest/esp32s3-test`; `PYTHONPATH=python:tests/functional QEMU_TEST_QEMU_BINARY=./build/qemu-system-xtensa QEMU_BUILD_ROOT=./build uv run --with pycotap python3 tests/functional/test_xtensa_esp32s3_cache_reject.py`; `PYTHONPATH=python:tests/functional QEMU_TEST_QEMU_BINARY=./build/qemu-system-xtensa QEMU_BUILD_ROOT=./build uv run --with pycotap python3 tests/functional/test_xtensa_esp32s3_sleep_wake.py`.
