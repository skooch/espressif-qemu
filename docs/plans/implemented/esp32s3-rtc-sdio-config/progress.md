# ESP32-S3 RTC SDIO Config Surface Progress

- 2026-04-18: Started the RTC SDIO config surface phase in `/Users/skooch/projects/tdeck-pro-rust/worktrees/esp32s3-rtc-sdio-phase6` on `codex/esp32s3-rtc-sdio-phase6`.
- 2026-04-18: Confirmed from the adjacent firmware's sleep path, the local sleep reference, and ESP-IDF `rtc_cntl_reg.h` that `RTC_CNTL_SDIO_CONF` remains an unsupported RTC offset even though the active light-sleep path writes it.
- 2026-04-18: Added explicit `RTC_CNTL_SDIO_CONF` register state, ESP-IDF-backed reset defaults, and source-backed write masks to the RTC model, and extended the RTC qtests to pin both masked readback and full-chip reset defaults.
- 2026-04-18: Verification passed: `git diff --check`; `ninja -C build qemu-system-xtensa tests/qtest/esp32s3-test`; `QTEST_QEMU_BINARY=./build/qemu-system-xtensa ./build/tests/qtest/esp32s3-test -p /xtensa/esp32s3/rtc/explicit-register-surface`; `QTEST_QEMU_BINARY=./build/qemu-system-xtensa ./build/tests/qtest/esp32s3-test -p /xtensa/esp32s3/rtc/reset-transitions`; `QTEST_QEMU_BINARY=./build/qemu-system-xtensa ./build/tests/qtest/esp32s3-test -p /xtensa/esp32s3/rtc`; `QTEST_QEMU_BINARY=./build/qemu-system-xtensa ./build/tests/qtest/esp32s3-test`; `QEMU_TEST_QEMU_BINARY=build/qemu-system-xtensa QEMU_BUILD_ROOT=build PYTHONPATH=python:tests/functional uv run --with pycotap python3 tests/functional/test_xtensa_esp32s3_cache_reject.py`; and `QEMU_TEST_QEMU_BINARY=build/qemu-system-xtensa QEMU_BUILD_ROOT=build PYTHONPATH=python:tests/functional uv run --with pycotap python3 tests/functional/test_xtensa_esp32s3_sleep_wake.py`.
